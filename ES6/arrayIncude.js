// Array includes() - checks if the item is an array
// useful in the conditional statement
// optianaly pass second arguments index

const groceries = ['milk', 'bread', 'meat'];

let random = 'milk';
// const isIncluded = groceries.includes(random);
// console.log(isIncluded);

if (groceries.includes(random)) {
  console.log(`Yes it on the list`);
}
